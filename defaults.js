/**
 * defaults.js — shared constants for content.js and popup.js
 * Loaded before the other scripts (see manifest.json and popup.html)
 * The starter list is spider-focused, but users can add any keywords they want.
 */

const DEFAULT_KEYWORDS = [
];
